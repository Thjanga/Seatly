spring:
  application:
    name: seatly
  datasource:
    url: jdbc:postgresql://localhost:5432/seatly
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
